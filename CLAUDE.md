# Repo guide

This repo holds two unrelated projects:

- **`textlyra/` — ACTIVE project.** AI-receptionist business (landing page + business docs).
  Read `textlyra/HANDOFF.md` FIRST — it contains full project state, decisions, links, and
  the next tasks. Work on branch `claude/ai-agent-landing-page-unbqog`. Keep `docs/index.html`
  (GitHub Pages copy) in sync with the site files in `textlyra/`.
- `veytra.py` + `fonts/` + `veytra_30day_plan.pdf` — separate, dormant PDF-generation project.
  Don't touch unless asked.
